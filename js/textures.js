(()=>{const c=document.querySelector("[data-textures-content]");if(!c)return;const e=v=>String(v??"").replace(/[&<>'"]/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[x]));const cats=[{key:"Curated textures and hand-crafted finishes",eyebrow:"01 / Curated finishes",title:"Curated & hand-crafted textures",intro:"Tactile surfaces developed to give each concrete form a distinct material character."},{key:"Curated solid shades",eyebrow:"02 / Curated shades",title:"Curated solid shades",intro:"A considered palette of solid tones designed to work across landscape and interior settings."}];fetch("../data/textures.json").then(r=>{if(!r.ok)throw Error(r.status);return r.json()}).then(items=>{c.innerHTML=cats.map(cat=>{const a=items.filter(x=>x.category===cat.key);if(!a.length)return"";const id=cat.key.toLowerCase().replace(/[^a-z0-9]+/g,"-");return `<section class="texture-category" aria-labelledby="${id}"><header class="texture-category__heading"><p class="site-eyebrow">${e(cat.eyebrow)}</p><h2 id="${id}">${e(cat.title)}</h2><p class="texture-category__intro">${e(cat.intro)}</p></header><div class="textures-grid">${a.map(x=>`<article class="texture-card"><div class="texture-card__image"><img src="${e(x.shadeImage)}" alt="${e(x.name)} finish" loading="lazy"></div><div class="texture-card__body"><p class="texture-card__id">${e(x.id)}</p><h3>${e(x.name)}</h3><p>${e(x.description)}</p></div></article>`).join("")}</div></section>`}).join("")}).catch(()=>{c.innerHTML='<p class="textures-error">We could not load the curated finishes right now. Please try again later.</p>'})})();